<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duzeming.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".image.deal.SecondActivity" />
        <activity android:name=".image.deal.MeiTuActivity" />
        <activity android:name=".image.deal.XiuXIuActivity" />
        <activity android:name=".image.deal.PixelEffectActivity" />
        <activity android:name=".image.deal.ImageMatrixActivity" />
        <activity android:name=".image.deal.RoundActivity" />
        <activity android:name=".image.deal.BitmapShaderActivity" />
        <activity android:name=".image.deal.ReflectActivity" />
        <activity android:name=".image.deal.MeshActivity" />
        <activity android:name=".animation.deal.AnimationMainActivity" />
        <activity android:name=".animation.deal.StartAnimationActivity" />
        <activity android:name=".animation.deal.SecondAnimationActivity" />
        <activity android:name=".animation.deal.ValueAnimationActivity" />
        <activity android:name=".animation.deal.AnimationOneActivity"/>
    </application>

</manifest>
